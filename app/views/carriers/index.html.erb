<h3><%= t("general.carriers") %></h3>

<%= form_tag carriers_path, :method => 'get', :id => "div_update_search" do %>
  <p>
    <%= t("general.search") %>: <%= text_field_tag :search, params[:search] %>
    <%#= submit_tag t("general.search"), :name => nil %>
  </p>
  <div id="div_update"><%= render 'fetchAll' %></div>
<% end %>

<p><%= link_to t("general.new"), new_carrier_path %></p>
